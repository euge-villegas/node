Este es mi primer trabajo con Node, alguna notas:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -h, --hasta    Es el numero limite hasta el que va a multiplicar
                                                          [number] [default: 10]
  -l, --listar   Muestra la tabla en lista [boolean] [required] [default: false]

```